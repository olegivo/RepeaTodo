CREATE TABLE Task (
    uuid TEXT NOT NULL PRIMARY KEY,
    title TEXT NOT NULL
);

CREATE INDEX task_uuid ON Task(uuid);

getTasks:
SELECT * FROM Task;

saveTask:
INSERT OR REPLACE INTO Task
VALUES ?;

getTask:
SELECT * FROM Task
WHERE uuid = :uuid;

deleteTask:
DELETE FROM Task
WHERE uuid = :uuid;
